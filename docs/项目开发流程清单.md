# 项目开发流程清单 - 实施指南

## 阶段一：工程初始化 (P0 - Week 1-2)
- [ ] 按照 `项目文件结构.md` 初始化 Rust + Tauri 2.0 工程。
- [ ] 配置 `Cargo.toml` 引入 `windows`, `tokio`, `sqlx`。
- [ ] 搭建 Chromium 146 编译环境，并成功生成第一个原生 `chrome.exe`。
- [ ] **验证点**：Rust 能够通过 `Command` 启动浏览器并指定 `--user-data-dir`。

## 阶段二：指纹链路与内核魔改 (P1 - Week 3-4)
- [ ] 在内核中实现 `SingletonFingerprint` 解析 `--bot-config-` 参数。
- [ ] 修改 `client_hints.cc` 实现 UA-CH 实时同步。
- [ ] 修改 `SkCanvas.cpp` 注入 +/-1 像素噪声。
- [ ] **验证点**：访问 CreepJS，确认 UA 与 Client Hints 完全一致，置信度上升。

## 阶段三：网络穿透与沙盒管理 (P2 - Week 5-6)
- [ ] 编写 Rust 异步 `ProxyBridge`，支持 SOCKS5 认证。
- [ ] 实现 UDD 目录的“冷热分离”清理逻辑。
- [ ] 实现 Cookie 的增量备份与压缩。
- [ ] **验证点**：在有密码的代理环境下，浏览器能正常上网且不泄露真实 IP。

## 阶段四：UI 集成与窗口管理 (P3 - Week 7-8)
- [ ] 完成 Element Plus 环境列表与指纹编辑器开发。
- [ ] 调用 `windows-rs` 实现宫格排列算法。
- [ ] 进行反检测“全绿”联调测试。
- [ ] **验证点**：多开 10 个窗口，执行宫格排列，CPU 占用稳定在预期范围内。

---
**参考规范：执行代码编写时请严格参照 `CLAUDE.md`。**
